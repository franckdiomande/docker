version: "3"
services:
    skyflow:
        container_name: skyflow
        build: ./
        ports:
            - 80:80
        volumes:
            - ./..:/var/www/skyflow

    composer:
        container_name: composer
        image: composer
        command: ["composer", "update"]
        volumes:
            - ./..:/app
